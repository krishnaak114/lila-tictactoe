// ESLint 9 flat config — replaces .eslintrc.json
// eslint-config-next 16.x exports native flat config arrays.
const nextConfig = require("eslint-config-next/core-web-vitals");
module.exports = nextConfig;
